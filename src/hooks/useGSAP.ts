'use client';

// Re-export from @gsap/react when we add that package, or use this pattern:
// useGSAP wraps GSAP context creation and cleanup for React components.
// Full implementation will be added in the animation phase.
export { useGSAP } from '@gsap/react';
